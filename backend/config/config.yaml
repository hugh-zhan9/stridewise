server:
  http:
    addr: ":8000"

security:
  internal_token: "change-me"

postgres:
  dsn: "postgres://stridewise:stridewise@postgres:5432/stridewise?sslmode=disable"

redis:
  addr: "redis:6379"

asynq:
  concurrency: 10

keep:
  phone_number: "change-me"
  password: "change-me"

strava:
  data_file: "../running_page/src/static/activities.json"

garmin:
  data_file: "../running_page/src/static/activities.json"

nike:
  data_file: "../running_page/src/static/activities.json"

gpx:
  data_file: "../running_page/src/static/activities.json"

tcx:
  data_file: "../running_page/src/static/activities.json"

fit:
  data_file: "../running_page/src/static/activities.json"

weather:
  qweather:
    api_key: "change-me"
    api_host: "change-me"
    timeout_ms: 3000

ai:
  provider: "openai"
  openai:
    api_key: "change-me"
    base_url: "https://api.openai.com/v1"
    model: "change-me"
    timeout_ms: 3000
    max_tokens: 400
    temperature: 0.2
